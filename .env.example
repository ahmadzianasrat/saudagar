VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Edge Function secrets (set these via `supabase secrets set`, NOT here —
# this file is only a reference for what needs to exist, never commit real values)
# HESABPAY_API_KEY=
# HESABPAY_WEBHOOK_SECRET=
